"# Fix Promos UI & Calculation Plan\n\n## User Review Required\nNo breaking changes, but the visual style of POS items will be completely redesigned based on your instructions. Please review the visual approach before proceeding.\n\n## Open Questions\n- For the tickets, do you prefer we show the discounted price next to the item, or show the original price and add a separate \"Discount\" line item below it? The plan assumes showing the discounted price directly next to the item.\n- For audits (Z-reports/X-reports), should discounts be aggregated and shown as a total \"Discount Given\" or is it sufficient that the net revenue matches the discounted totals?\n\n## Proposed Changes\n\n### CAJA_NEW/viewss/main_window.py\n- **UI Redesign**: Modify `build_products_grid`. For discounted items, we will remove the old string text layout. Instead, we'll place the \"PROMO (WAS $X)\" text explicitly on the TOP of the card, and leave the normal price text below. We will ensure the card height remains uniform using `min-height` and `max-height`. We'll also use a custom stylesheet for the promo card to make it look much better.\n- **Cart Add**: When a product is added, we will fetch its dynamically calculated discounted price from `product_buttons` (which caches the `nuevo_precio`) and insert *that* into the cart table `self.table` instead of `p.precio_actual`.\n- **Totals Calculation**: Update the logic calling `calcular_totales` and updating the UI so it properly accounts for the discounted prices shown.\n\n### CAJA_NEW/services/pos_service.py\n- **Ticket Generation**: Modify `generar_ticket_pdf` and `generar_ticket_desde_pedido` to print the calculated discounted price (`precio_final`) rather than the base price (`precio`). \n- **Audit Reports**: Ensure `generar_reporte_cuadre` pulls the correct final subtotal revenues (which already accounts for discounts correctly due to `factura.subtotal`, but we will verify no base-price logic is leaking into the audit).\n\n### WEB-MOBILE_BAR/bar_lounge_app/lib/features/menu/presentation/
<truncated 1129 bytes>